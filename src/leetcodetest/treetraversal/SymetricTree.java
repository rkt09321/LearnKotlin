package leetcodetest.treetraversal;

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class SymetricTree {
//    public boolean isSymmetric(TreeNode root) {
//      if(root==null)
//            return true;
//        return checkSym(root.left, root.right);
//    }
//    public boolean checkSym(TreeNode lft, TreeNode rht){
//        if((lft==null) && (rht==null))
//            return true;
//        if((lft==null) || (rht==null))
//            return false;
//        if(lft.val!=rht.val)
//            return false;
//        return ((checkSym(lft.left, rht.right)) & (checkSym(lft.right, rht.left)));
//    }
}