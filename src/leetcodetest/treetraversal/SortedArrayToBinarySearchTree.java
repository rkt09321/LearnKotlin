package leetcodetest.treetraversal;

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class SortedArrayToBinarySearchTree {
//    public TreeNode sortedArrayToBST(int[] nums) {
//     if(nums.length < 1) return null;
//        TreeNode ret = new TreeNode(nums[nums.length/2]);
//        ret.left = sortedArrayToBST(Arrays.copyOfRange(nums,0,nums.length/2));
//        ret.right = sortedArrayToBST(Arrays.copyOfRange(nums,(nums.length/2) + 1, nums.length));
//        return ret;
//    }
}