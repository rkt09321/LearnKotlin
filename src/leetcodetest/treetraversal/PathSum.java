package leetcodetest.treetraversal;

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class PathSum {
//    public boolean hasPathSum(TreeNode root, int targetSum) {
//      if(root == null && targetSum==0) return false;
//		//  checking tree is empty
//        return calcsum(root,targetSum);
//    }
//    public boolean calcsum(TreeNode root,int target){
//        if(root == null) return false;
//        target = target - root.val;
//        if(root.left == null && root.right == null){
//            return target == 0;     //checking path sum at leaf node
//        }
//        return calcsum(root.left,target)||calcsum(root.right,target);  //recursively traversing
//    }
}