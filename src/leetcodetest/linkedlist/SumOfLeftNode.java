package leetcodetest.linkedlist;

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class SumOfLeftNode {
//    public int sumOfLeftLeaves(TreeNode root) {
//        if(root == null)
//            return 0;
//        int left = helper(root,root.left);
//        int right = helper(root,root.right);
//        return left+right;
//    }
//    private static int helper(TreeNode parent,TreeNode current)
//    {
//        if(current == null)
//            return 0;
//        if(parent.left == current && current.left == null && current.right ==null)
//            return current.val;
//        return helper(current,current.left)+helper(current,current.right);
//    }
}